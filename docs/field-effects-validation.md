# Проверка огня, мороза и пара

15 сентября 2026. Правила: [field-effects-design.md](field-effects-design.md).

- `field_effects_test.gd`: 57 проверок, 0 ошибок. Подбор обеими сторонами, сроки, направленность сильного контакта, союзники, шесть порядков цепочки из трёх тел, следующий пропущенный бросок, остановка врага, защита, пар, восстановление снимка и последний тик волны.
- `simulation_test.gd`: 234 проверки, 0 ошибок.
- `boss_rules_test.gd`: 82 проверки, 0 ошибок. Старое ожидание бессрочного поджога заменено общим сроком.
- `spirits_test.gd`: 46 проверок, 0 ошибок.
- `runestones_test.gd`: 24 проверки, 0 ошибок.
- `clash_test.gd`: 14 проверок, 0 ошибок.
- `presentation_test.gd`: 19 проверок, 0 ошибок.
- `throw_energy_test.gd`: 1091 проверка, 0 ошибок.
- `magic_visuals_test.gd`: 10 проверок, 0 ошибок; визуализация не меняет снимок симуляции.
- `input_test.gd`: 0 ошибок сценариев ввода.
- Три теста `relay/test/reward-controller.test.mjs` прошли; сценарий дополнен ожиданием заморозки, блокировкой и восстановлением управления на связанном месте P4. `node --check` прошёл.
- `git diff --check` прошёл.

## Графическая проверка

`tests/element_capture.gd` записывает настоящее столкновение в симуляции и проверяет: одну реакцию пара, удаление обоих элементов и полное освобождение частиц после рассеивания. Пик — 33 частицы пара, пропущенных из-за заполненного пула — 0. Графические запуски 1440×900, 900×1000 и кадр источников завершились без ошибок скриптов и шейдеров.

- Видео со звуком: `work/field-effects/fire-frost-steam.mp4`, 4,17 с, 1440×900, 60 кадров/с.
- Исходные состояния: `steam-035.png`.
- Начало, облако, рассеивание: `steam-068.png`, `steam-084.png`, `steam-120.png`.
- Узкий кадр: `narrow-084.png`.
- Источники на поле: `sources-079.png`.

Фиксированный шаг записи проверяет визуальную последовательность, не производительность устройств. В headless-запусках Godot выдаёт окруженческую строку macOS `get_system_ca_certificates / ret != noErr`; перечисленные проверки завершаются успешно.

## Уточнение подбора пламени и новый визуал

15 сентября 2026, повторная проверка после изменения сроков:

- Подбор в T не наносит урон и не уменьшает горение. Первый тик — конец T+1; пламя сохраняется на следующий бросок, включая переход через награду в следующую волну.
- Передача от контактов сохраняет прежние сроки; повторный контакт не снимает отсрочку у подобравшего. Тушение удаляет отсрочку. Проверено сохранение до и после конца хода в JSON-снимке.
- `field_effects_test.gd`: 80 проверок, 0 ошибок.
- `simulation_test.gd`: 233 проверки, `boss_rules_test.gd`: 82, `spirits_test.gd`: 46, `magic_visuals_test.gd`: 10; все без ошибок проверок.
- Пламя использует `assets/field-fire-flipbook-v1.png`: созданный ImageGen атлас 4×4, плавное смешивание кадров и восходящее искажение в `shaders/field_flame.gdshader`. Источник — 3 слоя; носитель — 5 слоёв с коротким передним краем для видимости знака. Искры — фиксированный MultiMesh на 18 частиц, свет без теней. Прежние круглые частицы горения удалены.
- `fire_pickup_capture.gd`: реальный физический подбор → конец хода без урона → планирование → следующий бросок с пламенем → один тик → освобождение визуальных узлов. Проверено в 1440×900 и 900×1000. Видео: `work/fire-pickup/fire-pickup.mp4` (5 секунд, 60 кадров/с); кадры `fire-*.png`, `narrow-*.png`.
- Повторены кадр источников и столкновение с морозом в `element_capture.gd`; одна реакция пара, пик 33 частицы, пропущено 0, пул освобождается.
- Графические запуски без ошибок скриптов и шейдеров. `git diff --check` проходит. Фиксированная запись проверяет визуальную последовательность, не производительность на целевых устройствах.
